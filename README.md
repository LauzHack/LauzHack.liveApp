# LauzHack Livepage Editor

## How to use

* Have a unix system (OSX or Linux)
* Download the latest released jar of the app
* If using OpenJDK, first install `openjfx`
* Start the jar (by clicking on it or with `java -jar $jarPath`)